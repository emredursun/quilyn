{
  "moduleId": "TESTIM-M29",
  "moduleTitle": "Using the REST API in Tricentis Testim",
  "moduleUrl": "https://academy.tricentis.com/learn/learning-plans/145/tricentis-testim-product-consultant/courses/using-the-rest-api-in-tricentis-testim",
  "learningObjectives": [
    "Generate and use a Testim REST API key to authenticate requests.",
    "Explain the plan-level restriction on API access and the alternative authentication method for encrypted credentials."
  ],
  "estTime": "6 min",
  "studyGuide": [
    {
      "sectionTitle": "Enabling and authenticating API access",
      "elements": [
        {
          "type": "concept",
          "term": "Plan restriction",
          "description": "API access is a professional feature, available only to projects on the professional plan — it isn't something every project tier has access to by default."
        },
        {
          "type": "concept",
          "term": "Generating an API key",
          "description": "Go to Settings → API, click Generate API Key, and name it. The key's value is shown once; you must copy it immediately and click Done, since that display is the only time the raw value is visible."
        },
        {
          "type": "concept",
          "term": "Authenticating requests",
          "description": "The API key is used in the Authorization header as a Bearer token: Authorization: Bearer PAK-[your-api-key]. For calls involving encrypted credentials (e.g. POST /tests/run/{testId}), the user-access-key value is instead added to the X-User-Access-Key field."
        }
      ],
      "bulletPoints": [
        "API access = professional-plan-only feature.",
        "API key value is shown exactly once at generation time — copy it immediately.",
        "Auth: Authorization: Bearer PAK-[key]; encrypted-credential calls use X-User-Access-Key instead."
      ]
    }
  ],
  "examPitfalls": [
    {
      "title": "Navigating away before copying a newly generated API key",
      "trapDescription": "Generating an API key, clicking Done without copying the value, then having no way to retrieve it afterward.",
      "bestPractice": "The API key value is displayed only once, at generation time. Copy it immediately — if lost, a new key must be generated instead of recovering the old one."
    },
    {
      "title": "Using the Bearer token header for an encrypted-credentials call",
      "trapDescription": "Sending the same Authorization: Bearer header for a call that involves encrypted credentials, like POST /tests/run/{testId}.",
      "bestPractice": "For encrypted-credential calls, use the X-User-Access-Key header with the user-access-key value instead of (or alongside) the standard Bearer token, per that endpoint's requirements."
    }
  ],
  "practiceQuiz": [
    {
      "questionId": "Q1",
      "type": "single-select",
      "scenario": "Which plan tier is required for a project to use the Testim REST API?",
      "options": [
        { "id": "A", "text": "Any plan tier — API access is universal." },
        { "id": "B", "text": "The professional plan." },
        { "id": "C", "text": "Only the free trial tier." },
        { "id": "D", "text": "API access requires no plan at all, it's a separate purchase." }
      ],
      "correctOptions": ["B"],
      "hint": "It's explicitly gated to one tier.",
      "rationale": "API access is a professional feature, available only to projects on the professional plan — not available by default on every tier."
    },
    {
      "questionId": "Q2",
      "type": "single-select",
      "scenario": "When is the raw value of a newly generated API key visible?",
      "options": [
        { "id": "A", "text": "It's always retrievable later from Settings → API." },
        { "id": "B", "text": "Only once, at the moment it's generated — it must be copied immediately." },
        { "id": "C", "text": "It's emailed automatically every week." },
        { "id": "D", "text": "It's visible in every API response indefinitely." }
      ],
      "correctOptions": ["B"],
      "hint": "There's no 'view again' option afterward.",
      "rationale": "The API key value is displayed only at generation time — you copy it then and click Done. It cannot be viewed again afterward; a new key would need to be generated if it's lost."
    },
    {
      "questionId": "Q3",
      "type": "single-select",
      "scenario": "How is the Testim REST API key used to authenticate a standard request?",
      "options": [
        { "id": "A", "text": "As a query string parameter named apikey." },
        { "id": "B", "text": "In the Authorization header as a Bearer token: Authorization: Bearer PAK-[your-api-key]." },
        { "id": "C", "text": "It isn't used for authentication at all." },
        { "id": "D", "text": "As a cookie set manually by the developer." }
      ],
      "correctOptions": ["B"],
      "hint": "It follows the standard Bearer-token pattern, with a specific prefix.",
      "rationale": "The API key is used in the Authorization header as a Bearer token in the format Authorization: Bearer PAK-[your-api-key] — not as a query parameter or a manually-set cookie."
    }
  ],
  "quickRecap": [
    { "key": "Plan requirement", "value": "REST API access is a professional-plan-only feature." },
    { "key": "Generating a key", "value": "Settings → API → Generate API Key → copy immediately (shown once)." },
    { "key": "Standard auth", "value": "Authorization: Bearer PAK-[key]." },
    { "key": "Encrypted-credential calls", "value": "Use X-User-Access-Key with the user-access-key value." }
  ],
  "topics": [
    {
      "title": "Testim REST API (Tricentis docs)",
      "url": "https://docs.tricentis.com/testim/content/administration/api-access.htm",
      "duration": "docs"
    },
    {
      "title": "Encrypted Credentials (Tricentis docs)",
      "url": "https://docs.tricentis.com/testim/content/administration/encrypted-credentials.htm",
      "duration": "docs"
    }
  ]
}
