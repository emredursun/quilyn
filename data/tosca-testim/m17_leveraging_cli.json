{
  "moduleId": "TESTIM-M17",
  "moduleTitle": "Leveraging the Command Line Interface (CLI) in Tricentis Testim",
  "moduleUrl": "https://academy.tricentis.com/learn/learning-plans/145/tricentis-testim-product-consultant/courses/leveraging-the-command-line-interface-cli-in-tricentis-testim",
  "learningObjectives": [
    "Explain how to use the Testim CLI to streamline automation workflows.",
    "Install the Testim CLI and run tests from it.",
    "Use a configuration file to control which tests a CLI run executes and with what overrides."
  ],
  "estTime": "4 min",
  "studyGuide": [
    {
      "sectionTitle": "Installing and running via the CLI",
      "elements": [
        {
          "type": "concept",
          "term": "Installation",
          "description": "The Testim CLI is installed with npm: `npm i @testim/testim-cli`. It can then run commands either locally or remotely."
        },
        {
          "type": "concept",
          "term": "Why use the CLI at all",
          "description": "The CLI is the mechanism behind everything the editor/Test List local run can't do: cross-browser execution, parallel execution, CI/CD integration, and branch-scoped or data-driven runs across every data set (not just the first, as an editor run does)."
        },
        {
          "type": "concept",
          "term": "Configuration file",
          "description": "A config file (e.g. testimConfig.js) is created and passed to the CLI runner as a parameter, pointing to its path if needed. It can define the project ID, token, labels selecting which tests to run, the Selenium grid to use, and base URL overrides."
        }
      ],
      "bulletPoints": [
        "Install: npm i @testim/testim-cli.",
        "The CLI unlocks cross-browser, parallel, CI/CD, branch-scoped, and full data-driven runs.",
        "A config file (e.g. testimConfig.js) drives which tests run and how — project ID, token, labels, grid, base URL overrides."
      ]
    },
    {
      "sectionTitle": "Selected CLI flags worth knowing",
      "elements": [
        {
          "type": "list",
          "title": "Test selection & execution control",
          "items": [
            "--label / -l, --name / -n, --testId / -t, --suite, --suite-id, --test-plan — different ways to select which tests a run should execute.",
            "--rerun-failed-by-run-id — re-executes only the tests that FAILED in a specific previous remote run, instead of the whole suite.",
            "--intersect-with-label / --intersect-with-suite — runs the subset of tests matching BOTH criteria at once (an intersection), not just either one.",
            "--timeout — sets the execution abort threshold in milliseconds; defaults to 10 minutes, with a maximum of 3 hours.",
            "--retries — sets how many times a failed test automatically re-runs.",
            "--set-retention — configures how many days (1–10) results are retained before deletion."
          ]
        }
      ],
      "bulletPoints": [
        "--rerun-failed-by-run-id re-runs only what failed last time, not the full suite.",
        "Default execution timeout: 10 minutes; max configurable via --timeout: 3 hours.",
        "--intersect-with-label/--intersect-with-suite run an intersection of criteria, not a union."
      ]
    }
  ],
  "examPitfalls": [
    {
      "title": "Expecting a plain editor run to do everything the CLI can",
      "trapDescription": "Trying to achieve cross-browser or parallel execution from the editor Play button alone.",
      "bestPractice": "Cross-browser execution, parallel runs, CI/CD integration, and running every data-driven data set all require the CLI (or Scheduler) — not a local editor/Test List run."
    }
  ],
  "practiceQuiz": [
    {
      "questionId": "Q1",
      "type": "single-select",
      "scenario": "How is the Testim CLI installed?",
      "options": [
        { "id": "A", "text": "npm i @testim/testim-cli" },
        { "id": "B", "text": "By downloading a standalone .exe installer only." },
        { "id": "C", "text": "It comes pre-installed with every Testim project automatically." },
        { "id": "D", "text": "pip install testim" }
      ],
      "correctOptions": ["A"],
      "hint": "It's an npm package.",
      "rationale": "The Testim CLI is installed by running `npm i @testim/testim-cli` — it's a Node.js/npm package, not a standalone installer or a Python package."
    },
    {
      "questionId": "Q2",
      "type": "single-select",
      "scenario": "What can a Testim configuration file (e.g. testimConfig.js) control when passed to the CLI runner?",
      "options": [
        { "id": "A", "text": "Only the project's display theme (light/dark)." },
        { "id": "B", "text": "Project ID, token, labels selecting which tests run, the Selenium grid, and base URL overrides." },
        { "id": "C", "text": "The user's personal account password." },
        { "id": "D", "text": "Nothing — configuration files are not supported by the CLI." }
      ],
      "correctOptions": ["B"],
      "hint": "It's about which tests run, where, and against what.",
      "rationale": "A config file can include the project ID, token, labels to specify which tests to run, the Selenium grid, and base URL overrides — controlling what the CLI run actually executes and against what environment."
    },
    {
      "questionId": "Q3",
      "type": "single-select",
      "scenario": "Which Testim CLI flag lets you re-run only the tests that failed in a specific previous remote run, without re-running the tests that already passed?",
      "options": [
        { "id": "A", "text": "--rerun-failed-by-run-id" },
        { "id": "B", "text": "--retries" },
        { "id": "C", "text": "--disable-timeout-retry" },
        { "id": "D", "text": "--branch" }
      ],
      "correctOptions": ["A"],
      "hint": "It targets a specific prior run's failures, by that run's ID.",
      "rationale": "--rerun-failed-by-run-id re-executes only the tests that failed in a given previous run, letting you re-verify fixes without re-running the entire suite."
    },
    {
      "questionId": "Q4",
      "type": "single-select",
      "scenario": "What is the default execution timeout for a Testim CLI run, and what's the maximum it can be configured to via --timeout?",
      "options": [
        { "id": "A", "text": "Default 10 minutes; maximum 3 hours." },
        { "id": "B", "text": "Default 1 hour; maximum 24 hours." },
        { "id": "C", "text": "There is no default timeout." },
        { "id": "D", "text": "Default 90 seconds; maximum 10 minutes." }
      ],
      "correctOptions": ["A"],
      "hint": "It's measured in milliseconds under the hood.",
      "rationale": "A CLI run aborts after 10 minutes by default; --timeout can raise that threshold up to a maximum of 3 hours."
    },
    {
      "questionId": "Q5",
      "type": "single-select",
      "scenario": "You want to run only the tests that belong to a specific Suite AND also carry a specific label — the intersection of both, not either one alone. Which flag combination achieves this?",
      "options": [
        { "id": "A", "text": "--suite-id alone, since it already implies labels." },
        { "id": "B", "text": "--suite combined with --intersect-with-label." },
        { "id": "C", "text": "--test-plan, since it always intersects criteria." },
        { "id": "D", "text": "This isn't possible with the Testim CLI." }
      ],
      "correctOptions": ["B"],
      "hint": "There's a flag specifically named for intersecting selection criteria.",
      "rationale": "--intersect-with-label (or --intersect-with-suite) narrows a run to tests matching BOTH the suite and label criteria at once, rather than the broader union of tests matching either one."
    }
  ],
  "quickRecap": [
    { "key": "Install", "value": "npm i @testim/testim-cli." },
    { "key": "Why the CLI", "value": "Unlocks cross-browser, parallel, CI/CD, branch, and full data-driven runs." },
    { "key": "Config file", "value": "Project ID, token, labels, grid, base URL overrides — passed as a CLI parameter." },
    { "key": "Re-run failures only", "value": "--rerun-failed-by-run-id targets one prior run's failed tests." },
    { "key": "Timeout", "value": "Default 10 min, max 3 hours via --timeout." }
  ],
  "topics": [
    {
      "title": "Command line interface (CLI) (Tricentis docs)",
      "url": "https://docs.tricentis.com/testim/content/running-tests/the-command-line-cli/index.htm",
      "duration": "docs"
    }
  ]
}
