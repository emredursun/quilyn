{
  "moduleId": "TESTIM-M07",
  "moduleTitle": "Validating Test Steps in Testim",
  "moduleUrl": "https://academy.tricentis.com/learn/learning-plans/145/tricentis-testim-product-consultant/courses/validating-test-steps-in-testim",
  "learningObjectives": [
    "Demonstrate how to validate the CSS properties of an element during tests in Testim.",
    "Explain the method to validate data-driven actions such as clicking login, displaying login text, generating an email, and setting a password while testing an application.",
    "Perform custom validations for actions not included in the predefined steps menu, such as extracting a user-defined value using custom scripts.",
    "Verify whether a test case validation was successful based on custom validation steps."
  ],
  "estTime": "9 min",
  "studyGuide": [
    {
      "sectionTitle": "Validating CSS properties",
      "elements": [
        {
          "type": "concept",
          "term": "Validate CSS property",
          "description": "Checks any CSS property of an element. Property name takes a valid CSS property for the selected element (e.g. color, display, font-weight); Expected value is what that property should equal. The expected value can be a hardcoded literal (wrapped in quotes, e.g. 'red') or a parameter (entered by name, without quotes) — the same pattern used across other validation types."
        },
        {
          "type": "concept",
          "term": "Editing after creation",
          "description": "The property name and/or expected value can be changed later without re-selecting the element. If the element itself needs to change, hover the target element thumbnail and click Reassign, then click the new element in the AUT window — the same Reassign mechanism used on any other step."
        }
      ],
      "bulletPoints": [
        "Validate CSS property: Property name (a real CSS property) + Expected value (quoted literal or unquoted parameter name).",
        "Changing just the expected property/value doesn't require reselecting the element; changing the element itself uses Reassign."
      ]
    },
    {
      "sectionTitle": "Validating data-driven actions",
      "elements": [
        {
          "type": "concept",
          "term": "Data-driven validation in a real flow",
          "description": "A signup/login flow is a natural example of validating data-driven actions end to end: click Login triggers navigation, a Validate element text step confirms the resulting login text/heading displays correctly, a Generate random value or extraction step supplies a dynamically generated email, and a further validation confirms the password field was set/accepted as expected. Each generated or entered value gets its own validation, rather than only checking the final outcome."
        },
        {
          "type": "note",
          "text": "This ties together earlier modules directly: Generate random value/date for the dynamic data, Validate element text/visible for the resulting UI state, and Parameters to carry the generated value between steps."
        }
      ],
      "bulletPoints": [
        "Data-driven actions (click login, display login text, generate email, set password) each get their own validation step, not just a single end-of-flow check.",
        "This is where Generate random value/date, Parameters, and Validate element text/visible come together in one flow."
      ]
    },
    {
      "sectionTitle": "Custom validations for actions beyond the predefined menu",
      "elements": [
        {
          "type": "concept",
          "term": "Custom validations and custom actions",
          "description": "When a check or action has no equivalent in the predefined steps menu, Testim lets you write your own JavaScript — run either in the browser or outside it, in a Node.js context (a CLI validation/action) — to implement exactly the logic needed."
        },
        {
          "type": "concept",
          "term": "Pass/fail contract for custom validations",
          "description": "A custom validation step is meant to return a boolean: returning true makes the validation succeed, returning false makes it fail. This is the entire contract — whatever logic runs inside, only the final true/false return value determines the step's outcome."
        },
        {
          "type": "concept",
          "term": "Extracting a user-defined value for custom checks",
          "description": "The Extract Value step captures a value from the app (select the element to extract from; Testim creates the step and assigns that element) into a new parameter. That parameter can then be used inside a validation, a Set Text step, a custom step's code, or anywhere else a value is needed later — including as an input to a custom validation's own logic."
        }
      ],
      "bulletPoints": [
        "Custom validations run JavaScript in the browser or in Node.js (CLI) when no built-in step covers the check.",
        "The contract is simple: return true = pass, return false = fail.",
        "Extract Value captures a value into a parameter, reusable in later validations, Set Text steps, or custom code."
      ]
    },
    {
      "sectionTitle": "Verifying whether a validation succeeded",
      "elements": [
        {
          "type": "concept",
          "term": "Reading the step's status",
          "description": "After a run, a passed validation step shows its icon (upper-left corner of the step) in green, with a confirming message in the Properties panel. A failed validation step shows that same icon in red, with a failure message in the Properties panel — the same visual pattern applies to custom validation steps as to any predefined validation."
        }
      ],
      "bulletPoints": [
        "Passed step = green icon (upper-left) + success message in Properties.",
        "Failed step = red icon (upper-left) + failure message in Properties — same pattern for custom and predefined validations alike."
      ]
    }
  ],
  "examPitfalls": [
    {
      "title": "Forgetting Validate CSS property's expected value needs quotes for literals",
      "trapDescription": "Typing a literal expected value without quotes, which Testim then tries to interpret as a parameter name instead of a literal string.",
      "bestPractice": "Wrap a hardcoded literal in quotes (e.g. 'red'). Leave a parameter name unquoted — that distinction is how Testim tells the two apart."
    },
    {
      "title": "Returning something other than a clean boolean from a custom validation",
      "trapDescription": "Writing a custom validation that returns a truthy/falsy non-boolean value, or forgets to return at all, producing an ambiguous pass/fail result.",
      "bestPractice": "A custom validation's contract is explicit: return true to pass, return false to fail. Make sure the function actually returns one of these two values."
    },
    {
      "title": "Only validating the final outcome of a multi-step data-driven flow",
      "trapDescription": "Checking only that a signup completed successfully, without validating the intermediate generated email, the login text, or the password field along the way.",
      "bestPractice": "Validate each data-driven action individually (login click, resulting text, generated email, password set) so a failure is pinpointed to the specific step that broke, not just 'something in signup failed'."
    }
  ],
  "practiceQuiz": [
    {
      "questionId": "Q1",
      "type": "single-select",
      "scenario": "In a Validate CSS property step, how do you supply a hardcoded expected value versus a parameter?",
      "options": [
        { "id": "A", "text": "Hardcoded values are wrapped in quotes; parameter names are entered without quotes." },
        { "id": "B", "text": "Hardcoded values are entered without quotes; parameter names are wrapped in quotes." },
        { "id": "C", "text": "Both must always be wrapped in quotes." },
        { "id": "D", "text": "Parameters cannot be used in a Validate CSS property step." }
      ],
      "correctOptions": ["A"],
      "hint": "Quotes signal a literal string; no quotes signals a reference.",
      "rationale": "In the Expected value field, a hardcoded value is placed between quotes (e.g. 'red'), while a parameter value is entered by name without quotes — that distinction is how Testim tells a literal apart from a reference."
    },
    {
      "questionId": "Q2",
      "type": "single-select",
      "scenario": "What must a custom validation step return to make the step pass?",
      "options": [
        { "id": "A", "text": "The string \"pass\"." },
        { "id": "B", "text": "A boolean true." },
        { "id": "C", "text": "The number 1." },
        { "id": "D", "text": "Nothing — it always passes unless it throws an error." }
      ],
      "correctOptions": ["B"],
      "hint": "The contract is explicitly a boolean.",
      "rationale": "Custom validation steps are meant to return a boolean value: true makes the validation succeed, false makes it fail. A string, a number, or relying purely on the absence of a thrown error isn't the documented contract."
    },
    {
      "questionId": "Q3",
      "type": "single-select",
      "scenario": "How can a value be captured from the application and reused inside a later custom validation's logic?",
      "options": [
        { "id": "A", "text": "It cannot be reused — custom validations can only use hardcoded values." },
        { "id": "B", "text": "Using the Extract Value step to capture it into a parameter, then referencing that parameter later." },
        { "id": "C", "text": "By manually retyping the value into every step that needs it." },
        { "id": "D", "text": "Only by editing the project's base URL." }
      ],
      "correctOptions": ["B"],
      "hint": "It's the same Extract Value mechanism covered earlier in the path.",
      "rationale": "The Extract Value step captures a value from the app into a new parameter, which can then be used in a validation (including a custom validation's own code), a Set Text step, or other later steps — not something requiring manual retyping or unsupported."
    },
    {
      "questionId": "Q4",
      "type": "single-select",
      "scenario": "After a test run, how can you tell whether a specific validation step passed or failed?",
      "options": [
        { "id": "A", "text": "There is no per-step indicator — only the overall test result matters." },
        { "id": "B", "text": "The step's icon (upper-left) turns green with a success message, or red with a failure message, in the Properties panel." },
        { "id": "C", "text": "Passed steps disappear from the test entirely." },
        { "id": "D", "text": "Only custom validations show a status; predefined validations show nothing." }
      ],
      "correctOptions": ["B"],
      "hint": "It's a consistent green/red pattern, for both predefined and custom validations.",
      "rationale": "A passed validation shows a green icon in the step's upper-left corner with a confirming message in Properties; a failed one shows red with a failure message — the same pattern applies whether it's a predefined validation or a custom one."
    },
    {
      "questionId": "Q5",
      "type": "single-select",
      "scenario": "When testing a signup flow with a dynamically generated email and a click-login action, what is the recommended validation approach?",
      "options": [
        { "id": "A", "text": "Only validate that the final signup confirmation appears, ignoring the intermediate steps." },
        { "id": "B", "text": "Validate each data-driven action individually — the login click's result, the displayed login text, the generated email, and the password set — rather than only the end result." },
        { "id": "C", "text": "Skip validations entirely since the data is randomly generated." },
        { "id": "D", "text": "Use only a single Visual Validation step for the whole flow." }
      ],
      "correctOptions": ["B"],
      "hint": "Granular validation pinpoints exactly where a failure occurred.",
      "rationale": "Validating each data-driven action individually — not just the final outcome — pinpoints exactly which step broke when something fails, rather than leaving 'something in signup failed' as the only signal."
    }
  ],
  "quickRecap": [
    { "key": "Validate CSS property", "value": "Property name + Expected value; quoted = literal, unquoted = parameter." },
    { "key": "Changing the element", "value": "Reassign via the target element thumbnail — same as any other step." },
    { "key": "Data-driven validation", "value": "Validate each generated/entered value along a flow (login click, text, email, password), not just the end result." },
    { "key": "Custom validation contract", "value": "Return true = pass, return false = fail — runs in-browser or via Node.js (CLI)." },
    { "key": "Extract Value", "value": "Captures a value into a parameter, reusable in later validations and custom code." },
    { "key": "Status indicator", "value": "Green step icon + Properties message = pass; red + failure message = fail." }
  ],
  "topics": [
    {
      "title": "Validate CSS property (Tricentis docs)",
      "url": "https://docs.tricentis.com/testim/content/advanced-editing/validations/css-property-validation.htm",
      "duration": "docs"
    },
    {
      "title": "Validating using custom code (Tricentis docs)",
      "url": "https://docs.tricentis.com/testim/content/advanced-editing/validations/custom-code-1.htm",
      "duration": "docs"
    },
    {
      "title": "Extract Value Step (Tricentis docs)",
      "url": "https://docs.tricentis.com/testim/content/advanced-editing/extract-text.htm",
      "duration": "docs"
    }
  ]
}
